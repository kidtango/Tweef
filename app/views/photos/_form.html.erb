<%= form_with(model: photo, local: true) do |form| %>
  <% if photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class='field'>
		<%= form.label :image_url %><br />
    <%= form.text_field :image_id %><br />
	</div>
	<div class='field'>
		<%= form.label :tank_profile_id %><br />
    <%= form.text_field :tank_profile_id %><br />
	</div>
  <div class="actions">
    <%= form.submit %><br /><br />
  </div>
<% end %>
